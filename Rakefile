require_relative 'config/environment.rb'
require "sinatra/activerecord/rake"
#require 'graphviz'     # this is that "Graphviz" thingy in the Gemfile

desc "starts console"
task :console do
  Pry.start
end
